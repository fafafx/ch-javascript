// generar un id random en cada consulta, ese random sera id de cada receta max 3, que se podran guardar temporalmente, para generar un div donde se consoliden la impresion de las 3








totalRecetas.map(function (recetita, index, array) {
    recetita.id 
    recetita.nombre;
     
});


selectRecetas = document.getElementById("select-receta");
for(i = 2000; i <= 2050; i++){
    option = document.createElement("option");
    option.value = i;
    option.text = i;
    select.appendChild(option);
}